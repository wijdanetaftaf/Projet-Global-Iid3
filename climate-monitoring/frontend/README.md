# Climate Intelligence Frontend

Vite + React + TypeScript frontend for the Climate Monitoring demo.

Run:

1. `npm install`
2. `npm run dev`

Environment:

- Set `VITE_API_URL` in `.env` if backend runs on a different host.
